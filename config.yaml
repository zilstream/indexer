# Zilstream Indexer Configuration

server:
  port: 8080
  metrics_port: 9090

chain:
  name: "zilliqa"
  chain_id: 32769
  rpc_endpoint: "http://176.9.25.203:4202"
  block_time: 1s
  start_block: 3251550 # Close to first pair creation for testing

database:
  host: "localhost"
  port: 5432
  name: "zilstream"
  user: "melvin"
  password: ""
  ssl_mode: "disable"
  max_connections: 10

processor:
  batch_size: 100 # Adaptive batch size based on sync gap
  workers: 5 # Number of parallel workers
  requests_per_second: 50 # RPC rate limit
  max_retries: 3
  retry_delay: 1s

logging:
  level: "debug" # debug, info, warn, error
  format: "console" # json or console
