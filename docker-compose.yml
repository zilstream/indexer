version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: zilstream-postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: zilstream
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./internal/database/migrations:/docker-entrypoint-initdb.d
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Uncomment to run the indexer in Docker
  # indexer:
  #   build: .
  #   container_name: zilstream-indexer
  #   depends_on:
  #     postgres:
  #       condition: service_healthy
  #   environment:
  #     INDEXER_DATABASE_HOST: postgres
  #     INDEXER_DATABASE_PASSWORD: postgres
  #   volumes:
  #     - ./config.yaml:/app/config.yaml
  #   restart: unless-stopped

volumes:
  postgres_data: